
<%= simple_form_for(@device, html: { class: "form-horizontal"}) do |f| %>
  <%= f.error_notification %>

  <%= f.input :id, as: :text, input_html: { rows: "3" } %>

  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>